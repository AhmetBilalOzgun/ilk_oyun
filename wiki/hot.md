---
type: meta
title: "Hot Cache"
updated: 2026-09-16
verified: 2026-09-16
---

# Recent Context

> [!warning] Staleness
> Any product/build-state claim below older than 14 days is a **hypothesis, not a fact** (see Staleness Contract in `CLAUDE.md`). Verify against code, graph, or the running build before acting, then bump `verified:`.

## Last Updated
2026-09-16 — **RİTİM SEKTÖR STANDARDI (BEATSTAR) + İLK 5 BÖLÜM DENEYEREK-ÖĞREN TUTORIAL** (verified 2026-09-16): Kullanıcı: ritim "çok zor anlaşılıyor" → sektör standardını araştır+uygula; ilk 5 bölüm text-box'sız deneyerek-öğren tutorial. **(A) RİTİM YENİDEN YAZILDI** (`rhythm_minigame.gd`): eski = iki eksen aynı anda (5-yön jest + zamanlama) + sabit referans yok + yanlış yön TÜM komboyu öldürür (fail-soft ilkesine aykırı). Yeni = **Beatstar tarzı**: SABİT dikey isabet çizgisi + PERFECT/GOOD bandı, notalar sağdan sola akar, her nota kendi ikonunu taşır, pencere genişletildi, **FAIL-SOFT per-nota** (`_break_combo` kaldırıldı; ıska yalnız o notayı düşürür, kalanlar akar; `broke=_missed*2>tiles`). Motor sözleşmesi (setup/tile_resolved/finished) korundu. **(B) TUTORIAL:** yeni saf `Tutorial` (`scripts/meta/tutorial.gd`) = `TUTORIAL_LEVELS=5` + `rhythm_caps(level)` kademeli zorluk tavanı (L1 tek yavaş nota→L5 serbest) + `hint(key)` metin. `battle._tutorial_hint` = akıp giden tek satır `_flash`, ömür boyu bir kez (`Meta.seen_hints`). İpuçları rhythm/swipe/perfect/charge/orb/cards/build ilgili anda. `run_content` tutorial eşiği 3→5 (L1-L2 tek düşman, L3-L5 nazik; elite/heal/dallanma yalnız sonrası; `is_build_level` değişmedi). Test **2092 geçti**; smoke temiz. Telefonda elle: L1 okunurluk, çizgi hizası, yanlış-yön fail-soft, ipuçları bir kez. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **MAKRO KAPANIŞ**)
2026-09-16 — **MAKRO KAPANIŞ: RUN PUANI + KEŞİF/KODEKS + GÜNLÜK-HAFTALIK MEYDAN + 2 İPTAL** (verified 2026-09-16): Kullanıcı kararları. **(A) İKİ KALICI İPTAL:** orb push-your-luck ve elit "build sınayan özel kural" YAPILMAYACAK (roadmap işaretlendi). **(B) Numeric run-end score:** `RunScore.compute` (saf; campaign düğüm×10 / endless kat×12 + altın + relik×15 + arketip×25 + elit×40 + boss×100 + zafer×150). `RunState.battles_won/elites_won/bosses_won` (RunManager.report_battle_result artırır). `Meta.best_score`/`record_score`; özet panelde "★ PUAN … (rekor …)". **(C) Discovery + Kodeks:** `Meta.discovered`+`discover(key)` kalıcı; `battle` form(başlangıç+dönüşüm)/arketip/relik/düşman ilk karşılaşmada işler (`_on_choice_applied`, `_on_battle_requested`). `CodexData` = RunContent türevi tek kaynak; `scenes/codex.tscn`+`scripts/codex.gd` keşfedilen=isim+açıklama / değilse 🔒 ???, "KEŞİF n/total"; home "📖 KODEKS". **(D) Günlük/haftalık meydan (Faz 4 MVP, SUNUCUSUZ):** `Challenge.daily_seed`(YYYYMMDD)/`weekly_seed` deterministik → aynı gün herkes aynı harita (`map_rng.seed`); sabit zorluk `CHALLENGE_LEVEL=9`; `Meta.daily_best`/`weekly_best`. **Async sosyal STUB:** `Challenge.percentile` lojistik (REF=500/SPREAD=260) → "oyuncuların %N'ini geçtin". **Gerçek sunucu leaderboard hâlâ açık.** **(E) Wiki hatası düzeltildi:** Plazma arketip havuzu ZATEN koddaydı (`RunContent.plasma_archetypes`), "başlamadı" yanlıştı. Test **2075 geçti, 0 kaldı**; editor import 0 script hatası. Telefonda elle: kodeks + meydan özet + percentile; challenge zorluk & percentile sabitleri tune edilecek. → log.md, [[Makro Oyun — Yol Haritası]]
(önceki: **TATLIŞ AÇIK HAVA REVİZYONU**)
2026-09-16 — **TATLIŞ AÇIK HAVA REVİZYONU** (verified 2026-09-16): Kullanıcı kararı: mevcut sevimli alev animasyonunu koru, sade pixel art, beş açık hava köprü mekanı, alev/plazma için toplam sekiz görsel form. Menü, bölüm seçimi, ekipman ekranı ve savaş butonları krem/koyu yazıyla yenilendi. Kayan ritim şeridi yerine büyük soluktan belirgine geçen ok/TAP + küçük sonraki hareket kuyruğu + dolan halka/ŞİMDİ. Kör eden plazma düşman saldırılarını %35 ihtimalle tamamen ıskalatır; oyuncu fail-soft değişmez. Formlar mevcut ustalık/dışlayıcı arketip ilerlemesine bağlıdır. Kod+renderer doğrulandı; parmakla telefon denemesi henüz yok. → [[Tatlış Büyücüler ve Açık Hava Görsel Dili]]

2026-09-15 — **DEĞİŞİM PLANI KALAN 3 PARÇA: MASTERY CLAIM + STS NODE MAP + ENDLESS** (verified 2026-09-15): Wiki'de "ertelendi" işaretli 3 parça (plan `~/.claude/plans/starry-roaming-octopus.md`). **(1) Mastery CLAIM:** `add_mastery` artık ödülü otomatik uygulamaz (sadece XP+save); `MetaProgress.claimable_count()`+`claim_next()`; `home.gd` "🎁 TOPLA (n)" + ödül popup; battle zaferde "ödül hazır" toast. **(2) StS NODE MAP:** RunNode'a `next:Array[int]`/col/row + HEAL/TREASURE; yeni `RunMap` (sütun DAG + extend); `RunManager` graf gezer — 1 haleften=otomatik (lineer testler korunur), >1=**AWAITING_ROUTE**+`route_requested`+`choose(i)`; `RunContent.campaign_map(i,rng)` giriş→dallanan orta→boss→reward; `battle.gd` harita ekranı (bottom→top sütun, Line2D, erişilebilir=parıltı). `StageDef.linear` chain kenarlarıyla korundu. **(3) ENDLESS mode:** home "♾ ENDLESS" (`Meta.endless_run`); `RunContent.endless_map`+`RunMap.extender` (boss/reward yok, sonsuz uzar); `RunState.endless`/`depth`; ritim OVERRIDE = `maxf(adaptive, 1+depth·0.06)` (derinlikle monoton hızlanır, adaptive yavaşlatma ezemez); oda başı altın + ölümde banka (fail-soft) + `endless_best_depth` kalıcı. Test **253 geçti** (+24); home+battle smoke temiz. Telefonda elle. → log.md, [[Makro Oyun — Yol Haritası]], [[Mastery / Battle-Pass]]
(önceki: **ARKETİP GÖRSEL KİMLİK**)
2026-09-15 — **ARKETİP GÖRSEL KİMLİK (rozet + sprite tint)** (verified 2026-09-15): Kullanıcı — arketip kartları belirgin olsun + karakter/animasyon değişsin. (1) CHOICE'ta arketip seçeneği VBox: üstte "◈ ARCHETYPE ◈" PanelContainer rozeti (arketip aksan renginde) + kart (`_archetype_badge`). (2) `Archetype.tint` (Alev kızıl-turuncu / İnfaz altın-sarı / Patlama kor-kırmızı); `battle._party_tint` commit rengini verir, `_process` büyücü modulate'ini boyar (rest=tint, cast=parlak×tint) → tüm animasyonlar build'e göre renklenir. Test **223 geçti**; smoke temiz. Telefonda elle. → log.md, [[Build Arketipi — Enhancement, Replacement Değil]]
(önceki: **BATTLE-PASS / MASTERY TRACK**)
2026-09-15 — **BATTLE-PASS / MASTERY TRACK** (verified 2026-09-15): Kullanıcı yönü — arketipler/eşyalar rastgele değil, oynadıkça dolan barla **sırayla açılsın** (retention). Yeni `MasteryTrack` (`scripts/meta/mastery_track.gd`, pure): kümülatif eşikli ödül listesi (archetype/equipment/gold/crystal). `MetaProgress` + `mastery`/`claimed_tiers`/`unlocked_archetypes` + `add_mastery(n)` (dolan tier'ları sırayla claim eder, ödül uygular; kalıcı, çifte ödül yok) + `is_archetype_unlocked`. Gate: `RunState.unlocked_archetypes` (null=sınır yok), `ChoiceGenerator` yalnız açılanı sunar; `battle.gd` run başı `Meta.unlocked_archetypes` kopyalar + savaş zaferinde `add_mastery` (elite/boss bonus) + toast. `home.gd` mastery ProgressBar + sıradaki ödül. **Arketip erişimi 3 kapı: açık(mastery)+build-bölümü(cadence)+commit edilmemiş(dışlayıcı).** Track v1: 40🔥Alev/110👢/200🎯İnfaz/320💰/470💥Patlama/650🛡/900💎. Test **223 geçti**; home+battle smoke temiz. Denge+claim etkileşimi telefonda/sonra. → log.md, [[Mastery / Battle-Pass]]
(önceki: **ARKETİP = DIŞLAYICI SEÇİM**)
2026-09-15 — **ARKETİP = DIŞLAYICI SEÇİM (buff değil)** (verified 2026-09-15): Kullanıcı geri bildirimi — arketipler "seçimi ifade etmeliydi (burn/tek-hedef/alan)", stacklenen buff gibiydi. Karar (AskUserQuestion): **dışlayıcı commit** — run'da 1 arketip, diğer 2 kilit (`ChoiceGenerator._archetype_candidates`, loadout'ta arketip varsa sunmaz). **Davranış farkı, +sayı değil:** 🔥 Alev(DoT) / 🎯 İnfaz(execute 2.5+lifesteal, alan yok) / 💥 Patlama(yeni `basic_splash` 0.5 hook = normal saldırı alana yayılır + on_kill_aoe zinciri). `Archetype.icon` (🔥/🎯/💥). **Netleştirme: arketip DÖNÜŞÜM DEĞİL** — gerçek dönüşüm = kimlik-swap (Storm→Plazma); arketip form ÜSTÜNDE dışlayıcı yön. Açık: "ayrı arketip-seçim ekranı" (3'ü yan yana) hâlâ pool'da tekli. Test **206 geçti**. → log.md, [[Build Arketipi — Enhancement, Replacement Değil]]
(önceki: **BUILD CADENCE (task #4)**)
2026-09-15 — **BUILD CADENCE (task #4) — yakın-vade 1-4 TAMAM** (verified 2026-09-15): İçerik maliyeti dengesi — arketip teklifi yalnız build-bölümlerinde. `RunContent.is_build_level(i)` = i>=3 & (i-3)%4==0 → i=3,7,11,15,19. `RunState.allow_archetypes` gate (default true); `ChoiceGenerator._archetype_candidates` false→boş; `battle.gd` run başında `Meta.selected_level`'e göre kurar. Relic/dönüşüm/utility her bölümde açık; sadece arketip gate'lenir. Test **205 geçti**. **Makro yakın-vade task 1-4 (arketip sistemi, run-end summary, elite battle, cadence) TAMAM.** #5 (endless adaptive override) endless fazına ertelendi. UI parçaları telefonda elle. → log.md, [[Makro Oyun — Yol Haritası]]
(önceki: **ELITE BATTLE (task #3)**)
2026-09-15 — **ELITE BATTLE (task #3)** (verified 2026-09-15): Map'siz risk/reward (doküman §3). `RunNode.Type.ELITE` + `is_elite()`, `is_battle()` içerir, RunManager battle olarak sunar. `StageDef.linear` opsiyonel `elite_set` → boss'tan önce ELITE+CHOICE (mevcut imza korundu). `RunContent.stage_nodes` i>=3'te `_elite_set` (mauler+wraith HP×1.7/DMG×1.3). Yenince orb `ELITE_ORB_MULT=2` katlanır. UI: "☠ ELİT SAVAŞ" turuncu + flash. "Build'i sınayan özel kural" ERTELENDİ. Test **198 geçti** (`_test_elite_node`); smoke temiz. → log.md, [[Makro Oyun — Yol Haritası]]
(önceki: **RUN-END SUMMARY (task #2)**)
2026-09-15 — **RUN-END SUMMARY (task #2)** (verified 2026-09-15): Run kapanış katmanı (doküman §5) — `battle._show_run_summary(won, cry)` eski tek-satır run-end'i değiştirdi. Gösterir: başlık (🏆/💀) + **BUILD** (loadout `form_display_name` = "Alev Kor Büyücü" + arketip adları) + toplanan relikler + **KAZANILAN** (+gold💰 +crystal💎). `_summary_label` helper. **Numeric score + best score + new discovery ERTELENDİ** (skor formülü + discovery = faz-2 kararı yok). Telefonda elle. → log.md, [[Makro Oyun — Yol Haritası]]
(önceki: **BUILD ARKETİP SİSTEMİ (task #1)**)
2026-09-15 — **BUILD ARKETİP SİSTEMİ (task #1 kodlandı)** (verified 2026-09-15): Makro build keşfinin çekirdeği. Yeni `Archetype` (`scripts/rpg/archetype.gd`) = run build katmanı; **kimlik-swap'ı DEĞİŞTİRMEZ, üstüne biner** (enhancement, bkz [[Build Arketipi — Enhancement, Replacement Değil]]). Combat gücü mevcut `RelicSet` kancasıyla akar: `RunState.relic_set()` her loadout'un `archetype_effects()`'ini de RelicSet'e ekler → motor değişmeden çoğu efekt çalışır. **`ChoiceOption.Kind.ARCHETYPE`** (apply→`loadout.add_archetype`) + `ChoiceGenerator._archetype_candidates` (COST 24). `generate()` yeniden yapılandırıldı: dönüşüm ana andır → build havuzu onu ezmesin diye AYRI, uygunsa 1 dönüşüm slotu GARANTİ. **Ember havuzu (`run_content.ember_archetypes`):** Alev Yükü(burn_dmg_amp×1.6+dot_amp×1.5+burn_spread), Öldürücü Ritim(charge_gain_mult×1.5+execute×2.2), Zincir Patlama(on_kill_aoe×0.6+burn_spread). **Tek yeni hook `on_kill_aoe`** (`turn_manager._apply_action` ölüm dalı): party öldürünce komşulara `final_damage×amount`. UI: CHOICE 🔥 kart + ateş rengi + parıltı; `RunLoadout.form_display_name()` "Alev Kor Büyücü". Yeni class_name için global cache `--editor --quit` ile yenilendi. Test **189 geçti**; battle.tscn smoke temiz. Telefonda elle doğrulanmalı. → log.md, [[Makro Oyun — Yol Haritası]]
(önceki: **MAKRO YOL HARİTASI + BUILD ARKETİP KARARI**)
2026-09-15 — **MAKRO YOL HARİTASI + BUILD ARKETİP KARARI** (verified 2026-09-15, plan/kod yok): Kullanıcı "Makro Oyun — Tasarım Önerileri" dokümanını sundu; kritik + karar oturumu. Makro motivasyon = **build keşfi**. **Kararlar:** (1) Build = **enhancement, replacement DEĞİL** — kimlik-swap (Ember→Storm rünü→Plazma) DURUR; üstüne wave-arası CHOICE'ta **Burn/Crit/Explosion arketipi** seçilir → aktif formu modifiye eder ("Burn Ember"), transform'la stackler. Aynı 3 arketip Plazma için de. Uygulama **B yolu** = ayrı arketip-seçim ekranı + arketip başına ayrı efekt havuzu (reddedilen A = relic aile-etiketi). (2) İçerik pahalı → **her 3-5 bölümde bir** build-node, kalan bölümler **stat meta** (emniyet ağı korunur). (3) **Endless'ta ritim sürekli hızlanır** (adaptive override; campaign'de adaptive kalır). (4) **Node map ERTELENDİ** ama backlog'da kesin; elite battle map'siz eklenebilir. (5) Run-end summary erkene; **orb push-your-luck ERTELENDİ**. **Yakın vade task:** build arketip sistemi(B) → run-end summary → elite battle → cadence wiring. → log.md, [[Makro Oyun — Yol Haritası]], [[Build Arketipi — Enhancement, Replacement Değil]]
(önceki: **ADAPTIVE RİTİM ZORLUĞU**)
2026-09-15 — **ADAPTIVE RİTİM ZORLUĞU (piano tiles hızı oyuncuya uyar)** (verified 2026-09-15): Kullanıcı — oyun reflekse dayalı; 20 de 60 yaş da zevk alsın diye piano tiles HIZI oyuncunun oynayışına uysun. Hem genel profil hem oturum-bazlı (kötü gün / eli başkasına verme). **Adaptive olan TEK şey ritim hızı.** `MetaProgress` iki hız-skill'i tutar: `rhythm_skill` KALICI global (kaydedilir, `RHYTHM_GLOBAL_GAIN=0.020`/cast — yavaş) + `_rhythm_session` OTURUM (RAM, kaydedilmez, `SESSION_GAIN=0.080`/cast — hızlı tepki). Oturum ilk kullanımda kalıcıdan tohumlanır, uygulama kapanınca sıfırlanır. `rhythm_speed_scale()` = `%40 kalıcı + %60 oturum` (`SESSION_WEIGHT=0.6`), `[MIN=0.6, MAX=1.6]` clamp. `battle._on_input_requested` `speed_scale = wave_scale × Meta.rhythm_speed_scale()`; `_on_rhythm_finished` → `Meta.record_rhythm_result(combo_score, broke)`: `err = score − 0.82`, kırılınca `err=min(err,−0.30)`; iyi→hızlan, zorlandı→yavaşla (kolaylaşma agresif). `rhythm_minigame.setup` clamp tabanı `1.0`→`SPEED_MIN_SCALE=0.6` (base ALTINA inip yeni/60 yaş için gerçekten yavaşlar). Test **179 geçti** (`_test_rhythm_adaptive`). Telefonda elle doğrulanmalı. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **CHOICE EKRANI YENİDEN DÜZEN + YAZILAR BÜYÜTÜLDÜ**)
2026-09-14 — **CHOICE EKRANI YENİDEN DÜZEN + YAZILAR BÜYÜTÜLDÜ** (verified 2026-09-14): Kullanıcı — yazılar çok küçük + wave-arası seçim ekranı çok karışık. **(1)** Global `project.godot [gui] theme/default_font_size=32`; battle status 18→32/flash→40/skill btn→30, rhythm hint→28, orb_board 26→34 & 16→24/26, home btn→34. **(2)** CHOICE dikey liste → **tek yatay satır**: SOL **CAN AL** (kırmızı `+`, `HEAL_FIXED_COST=10` orb, party +`25`; `_on_heal_fixed`→heal+`skip_choice`) · ORTA **3 kutu yan yana** (`_choice_icon`+ad+bedel, StyleBoxFlat kutu) · SAĞ **PAS** (`»»`). Reroll ikincil altta. `_style_button` width+font_size param'lı; `_choice_label` silindi. Telefonda elle doğrulanmalı. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **TÜRKÇE FONT: PIXELOPERATOR → PIXELIFY SANS BOLD**)
2026-09-14 — **TÜRKÇE FONT: PIXELOPERATOR → PIXELIFY SANS BOLD** (verified 2026-09-14): Kullanıcı — genel fontta Türkçe karakter sıkıntısı. Kök neden: `PixelOperator8-Bold` `ğ Ğ İ ş Ş` glyph'lerini içermiyor (tüm PixelOperator ailesi içermiyor), sistem fallback pixel-olmayan fonta düşürüyordu. Çözüm: **Pixelify Sans** (chunky pixel, tam Türkçe, OFL) — variable'dan `wght=700` static Bold instance (`assets/fonts/PixelifySans-Bold.ttf`). 4 script `PIXEL_FONT` preload güncellendi (`battle`/`home`/`orb_board`/`rhythm_minigame`); `project.godot` yeni `[gui] theme/custom_font` = global varsayılan → yalnız font_size override eden ekranlar (characters/level_select/turn_debug_overlay) da pixel+Türkçe. Eski font+import silindi. `.import` editör açılınca üretilir; telefonda elle doğrulanmalı. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **RİTİM WAVELER GEÇTİKÇE HIZLANIR**)
2026-09-14 — **RİTİM WAVELER GEÇTİKÇE HIZLANIR** (verified 2026-09-14): Kullanıcı — piano-tiles her tur (wave) geçtikçe hızlansın. `TurnManager.round_index` sayacı (start_battle→0, her `_start_round`→+1). `battle._on_input_requested` `speed_scale = 1 + waves_passed·0.12` (`RHYTHM_SPEEDUP_PER_WAVE`) hesaplayıp `RhythmMinigame.setup(combo_len, rect, speed_scale)`'a verir. Minigame `_speed=SPEED_BASE·k`, `_beat=BEAT_BASE/k` (uzaysal aralık sabit, notalar hızlanır; tavan `SPEED_MAX_SCALE=2.2`). İlk wave 1.0x. Telefonda elle doğrulanmalı. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **UX CİLA: RİTİM YAVAŞLATMA + ULT GÖSTERİSİ + SEMBOLİK BECERİ BUTONLARI + SADE BUFF METNİ**)
2026-09-14 — **UX CİLA: RİTİM YAVAŞLATMA + ULT GÖSTERİSİ + SEMBOLİK BECERİ BUTONLARI + SADE BUFF METNİ** (verified 2026-09-14): 6 kullanıcı isteği. **(1)** Piano-tiles yavaşlatıldı (`rhythm_minigame.gd`: `BEAT 0.52→0.64`, `SPEED 640→480`, `_lead→0.72`, pencereler biraz geniş). **(2)** Ultimate finisher görsel olarak ayrıldı — `_ultimate_flourish` = tam ekran flaş (`_screen_flash`) + kamera sarsıntısı (`_screen_shake`) + aoe düşman parlaması, patlama ölçeği ult 2.1 vs temel 1.5; yalnız `requires_charge` finisher'da. **(3)** Beceri butonları sembolik: büyük ikon önce (`_skill_icon` 🔥🌋⚡🌩🌟) + element rengi (`_skill_color`); **sabit jest dizisi `[◀→▶→●]` butondan KALDIRILDI** (dizi zaten rastgele — "eski fixed şekiller"), `characters.gd._kit_text`'ten de silindi (yerine `passive_text`). **(4)** CHOICE cila: menü `y=720`/`separation=20`/`autowrap` (spacing fix), tür-renkleri (`_choice_color`), **dönüşüm kartı mor parıltı** (`_pulse_button`); rün etiketi "Storm Rününü Al — X ol". **(5)** Buff metinleri jargonsuz sadeleşti (Shatter/DoT/Storm/cast→sade cümle). `run_project` runtime temiz. Ritim hızı/ult gösterisi/parıltı telefonda elle doğrulanmalı. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **RİTİM → KOMBO DÖVÜŞÜ (per-tile büyü + finisher + haptik + rastgele dizi)**)

2026-09-14 — **RİTİM → KOMBO DÖVÜŞÜ (per-tile büyü + finisher + haptik + rastgele dizi)** (verified 2026-09-14): Ritim minigame tek-toplu-vuruştan **kombo**ya çevrildi (kullanıcı: tatmin). Beceri seçilince **rastgele** jest dizisi kayar (5 yön; sabit `input_sequence` değil); her tutturulan tile bir **fireball vuruşu** + yükselen hasar sayısı; **son tile FINISHER** (en büyük ölçek/`ult` pozu/en büyük hasar, `FINISHER_WEIGHT=2.0`). **Yanlış yön/ıska → KOMBO KIRILIR** (kalan düşer, finisher gider, sıra rakibe; o ana dek vuranlar sayılır, fail-soft floor korunur). **MÜKEMMEL tile'da haptik** `Input.vibrate_handheld(40)`. **Kombo uzunluğu run içinde uzar:** `_combo_length()=clampi(2+int(node_index/2),2,6)`. **Motor saflığı:** `RhythmMinigame` başarıyı `combo_score∈[0,1]`'e indirger → `battle` `mult=max(miss,perfect·score)` → **yeni** `TurnManager.submit_input_multiplier(mult,quality)` → mevcut `_apply_action` (şarj/DoT/ölüm/battle-end tek olay). Per-tile sayı=`PF·fraction`, aggregate FX `_suppress_aggregate_fx` ile bastırılır. Sinyaller `tile_resolved(...)`+`finished({combo_score,broke,tiles})`; enum `submit_input` enemy/no-input için korundu. Test **172/172**; `run_project` runtime temiz. Haptik/finisher telefonda elle doğrulanmalı. Alev büyücüsü asset promptu: `wiki/design/alev-buyucu-asset-prompt.md`. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **RİTİM GESTURE-TILE MİNİGAME + ORB LINE-GATE BOARD + PIXEL SAVAŞ UI**)

2026-09-14 — **RİTİM GESTURE-TILE MİNİGAME + ORB LINE-GATE BOARD + PIXEL SAVAŞ UI** (verified 2026-09-14): 6 kullanıcı isteği + kullanıcı cila pası. **(1) Ritim minigame = JEST+RİTİM hibridi** (`RhythmMinigame`, `scripts/rpg/rhythm_minigame.gd`): eski serbest yakalama KALDIRILDI; beceri adımları pixel TILE (`●`=TAP, `◀▶▲▼`=SWIPE) olarak kayar, çizgide DOĞRU JESTİ yap → yön yanlış=MISS("YANLIŞ YÖN!"), doğru+±0.11s=PERFECT("MÜKEMMEL!"), ±0.26s=GOOD("HARİKA!"), pencere geçti=MISS("ISKA!"); `_input` press→drag≥35px=yön/release=TAP, en yakın tile'a eşler; BPM~115 (`BEAT=0.52`,`SPEED=640`,`_lead=0.55`); `_aggregate`→`finished`→`submit_input` (motor jesti görmez, yalnız `InputEvaluator.Result`). **(2) Orb board line-gate** (`orb_board.gd`): negatif/<1 çarpan YOK, çarpan=YATAY ÇİZGİ (gate), peg 6→3 (strateji>şans); `_build_gates` her setup 3 gate'i RASTGELE kurar (havuz `[2,3,4,2,3,2]` shuffle + x jitter); gate'ten geçen orb TOP SAYISI çoğaltır (mult−1 klon, `MAX_ORBS=90`), puan=toplanan×ORB_VALUE. **(3) Görsel:** kazanma "victory" animasyonu kaldırıldı; büyük çizim (WIZARD_SCALE 0.9, ENEMY_SCALE 2.15), düşmanlar yatay dizi + uçanlar (ejderha/okçu) havada+hover; kare impact→YUVARLAK (disk+halka); rün dönüşümü reveal'ı SONRAKİ savaş ilk turunda (`_reveal_transform`). **(4) Pixel UI cilası (kullanıcı):** `PixelOperator8-Bold` her yerde, StyleBoxFlat beceri butonları, dinamik zemin/arka plan (`_ground_y`), `GROUND_Y`=1285, `ENEMY_FLOAT_LIFT`=100, debug overlay F1'e gizli. Test **164/164**; 3 script headless compile temiz. Ritim/board/hover telefonda elle doğrulanmalı. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **REDESIGN: KİMLİK-SWAP FORMLAR + AKTİF GİRDİ + EMBER→PLAZMA DİLİM + EKİPMAN**)

2026-09-13 — **REDESIGN: KİMLİK-SWAP FORMLAR + AKTİF GİRDİ + EMBER→PLAZMA DİLİM + EKİPMAN** (verified 2026-09-13): Yeni source-of-truth spec uygulandı (Cup Heroes tarzı roguelite RPG). Çekirdek döngü **FIGHT → EXECUTE (aktif girdi) → KILL → EARN ORBS → MULTIPLY (board) → CHOOSE → TRANSFORM → FIGHT**. 3 karar: **(1) Kimlik-swap dönüşüm** — `MageForm` (temel büyü+ultimate+pasif+aktif girdi). Storm rünü Ember'i **Plazma'ya KOMPLE dönüştürür** (combo matrisi silindi); `SkillCatalog`=forms+transforms, `RunLoadout.current_form`, `ChoiceOption.ACQUIRE_RUNE`, `RunManager.transformed`. **(2) Aktif girdi geri geldi (çizim DEĞİL)** — `InputSequence` (TAP/SWIPE) + saf `InputEvaluator` (PERFECT/GOOD/MISS, fail-soft: MISS=taban). `TurnManager` `AWAITING_INPUT`+`submit_input(result)`; `BattleConfig` 1.5/1.25/1.0 çarpanlar; `battle.gd` jesti `_unhandled_input`'ta yakalar, 3s timeout. **(3) Ember→Plazma dilim** — tek Kor(Ember) büyücü, `[BATTLE,CHOICE]×3→BOSS→REWARD`, 3 arketip+Kül Ejderi, `LEVEL_COUNT`=5; 3-büyücü/6-combo/20-seviye PARK. **(4) Ekipman** — `Equipment` (9 parça/3 slot, Relic hook deseni), Meta `owned_equipment`/`equipped`, savaşta `RelicSet`'e eklenir; hook'lar burn_dmg_amp/shatter_dmg_amp/reflect/speed_flat/max_hp_flat; shop `characters.gd`'de. Test **164/164**; 4 sahne headless smoke temiz. Fizik board + jest telefonda elle test edilmeli. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **CORE LOOP YENİDEN TASARIM** — çizim kaldırıldı, combo matrisi, orb board, relic'ler)

2026-09-13 — **CORE LOOP YENİDEN TASARIM (BUILD→FIGHT→INTERACT→MODIFY→POWER SPIKE)** (verified 2026-09-13): Kullanıcı yönü — oyun "hangi build'i kurup nasıl kullanırım" etrafına döndü. **(1) Çizim/QTE TAMAMEN KALDIRILDI** ("çizime baybay"): `TurnManager` QTE state'i silindi, beceri menüden seçilince ANINDA çözülür, cast çarpanı (`skill.qte_bonus_multiplier`) hep uygulanır (ıska yok; zorluk build/düşman/relic'ten). Recognizer/rune_templates/qte_minigame/rune_trail/rune_db/charge_button **silindi**. **(2) Combo TAM MATRİSİ**: 6 element çifti (ember/frost/gale/storm → Plazma/Buz Fırtınası/Siklon/Buhar/Ateş Fırtınası/Kar Fırtınası), iki farklı rün tutulunca EMERGENT açılır + `combo_discovered` → "YENİ BİRLEŞİM" flash. **(3) Orb = Cup Heroes fizik board**: yenilen düşman başına 1 orb (`RunState.orbs`), CHOICE'ta Plinko board'a (`scripts/orb_board.gd`, RigidBody2D + peg + Area2D slot) DOKUN-BIRAK ile dökülür → çarpan → DRAFT PUANI (`OrbBoardResult.score`) → kart seç/reroll(15)/pas. **Combat orb harcamaz** (kullanıcı: harcama sadece kartı hak etmek için). **(4) Relic kartları** (`Relic`/`RelicSet`, `ChoiceOption.RELIC`): 8 kural-değiştiren, run boyu kalıcı, motor hook noktalarında sorar — burn_spread(Wildfire)/post_storm_amp(Overcharge)/frozen_amp(Permafrost)/charge_gain_mult(Kondansatör)/dot_amp(Köz)/lifesteal(Kan Bağı)/execute(İnfaz)/shatter_pierce(Delici). **Ertelendi:** aktif savunma (dodge/block), beceri-başına mikro-oyun. Test **113/113**; battle.tscn headless smoke temiz; fizik board elle test edilmeli. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **20 seviyelik zorluk eğrisi**)

2026-09-12 — **20 seviyelik zorluk eğrisi** (verified 2026-09-12): İlk 3 tutorial bölümü **DEĞİŞMEDEN** (nötr, s=1+0.15i, byte-özdeş) `LEVEL_COUNT` 3→**20**. i>=3 içerik veri-tablosundan (`_LEVELS`, `run_content.gd`): arketip spawn'ları + savaş başına **zaaf teması**, iki ayrı ölçekle büyür — HP dik (1.30+0.18/lv, boss DPS-check→POWER+combo), hasar yumuşak (1.30+0.12/lv, →CAN+heal). **Temalar mono büyücüye adil** (draftla her rün açılır; zaaf bonus, kapı değil): `storm`(Shatter-zayıf→storm draft+combo), `elem`(element-zayıf), `ward`(Shatter-dirençli), `aegis`(element-dirençli+Shatter-zayıf→storm/shatterwave şart). Tier: T1(4-6) zaaf tanıt, T2(7-10) combo+hayatta kalma, T3(11-15) karışık, T4(16-20) ustalık, L20 capstone Boşluk Devi (aegis, ~785hp/53dmg). Arketipler grunt/runner/archer/brute/wraith/warden/mauler + 6 boss. `level_select.gd` 20 buton için ScrollContainer. Test 112/112. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **3 mono büyücü seçimi + tek-karakter tutorial + şarj decay**)

2026-09-12 — **3 mono büyücü seçimi + tek-karakter tutorial + şarj decay** (verified 2026-09-12): Oyunun ilk kısmı tasarlandı (kullanıcı). **Karakter seçimi:** oyun başı 3 mono-element büyücüden biri — **Kor**(Ateş/ember), **Buz**(Frost/frost), **Yel**(Rüzgar/gale). `Meta.selected_character` kalıcı; home varsayılan seçer + gösterir, characters ekranında SEÇ butonu. **İlk 3 bölüm TEK karakterle** (`RunContent.single_party`), kolay: `LEVEL_COUNT`=3, ölçek 0.15/sv, NÖTR düşmanlar, boss 100HP. **Combo modeli (ortak katalizör storm):** her büyücü element rünüyle başlar, storm draft edince birleşimi açılır — ember+storm→Plazma(yakma), frost+storm→Buz Fırtınası(stun), gale+storm→Siklon(push). tempest(frost+gale) kaldırıldı. **Wave-arası şarj decay:** şarj eskiden her savaşta sıfırdan başlıyordu; artık HP gibi savaşlar (wave) arası `RunLoadout.charge`'da taşınır ama her geçişte %20 düşer (`store_charge`, bileşik, asla<0). Savaş-içi tur-sonu decay YOK. Test 112/112. → log.md, [[Turn-Based Savaş ve QTE]]
(önceki: **Rün skorlama toleransı gevşetildi**)

2026-09-12 — **Rün skorlama toleransı gevşetildi** (verified 2026-09-12): Kullanıcı "çizim toleransı çok düşük, puanlar çok düşük". `RuneTemplates.MAX_DIST` 0.40 → 0.60 (skor = 1 - dist/MAX_DIST; eğim düşürüldü). Headless probe: %12-16 parmak titremesi 0.53-0.79 → 0.68-0.86; yanlış şekil hâlâ ~0.06-0.11 (stroke-sayısı cezası ayrı, ayrıştırma korundu). Test 108/108. → log.md, [[Rün Çizim Mekaniği]]
(önceki: **FIX: wave-sonu crash**)

2026-09-10 — **FIX: wave-sonu crash** (verified 2026-09-10): `battle.gd._process`'te `tick`/`advance_turn`/`submit_drawing` savaşı bitirince sinyal zinciri `tm`'i null/yeni yapıyordu; sonraki `tm.state` erişimi crash (DoT/son vuruş → "bazen"). Çözüm: yerel `cur_tm`, her savaş-bitiren çağrıdan sonra `tm != cur_tm` ise frame'den çık. Test 108/108. → log.md
(önceki: **QTE skorlama: doğruluk+hız kalitesi, çoklu-stroke, övgü + maks hasar**)

2026-09-10 — **QTE skorlama: doğruluk+hız kalitesi, çoklu-stroke, övgü + maks hasar** (verified 2026-09-10): Çizim artık GERÇEKTEN puanlanıyor (kullanıcı "classify çalışmıyor/kolay" şikayeti). Yeni `RuneTemplates.score` = geometrik doğruluk (ortak kutuya normalize → konum/ölçek bağımsız, yön-bağımsız stroke eşleme, stroke sayısı cezası) → [0,1]. Kimlik `recognize`, özen `score`. `TurnManager`: kalite = accuracy×hız; **eşik 0.45 altı = ıska (fail-soft taban)**, üstü kaliteyle ölçekli bonus (1.0→qte_bonus_multiplier). `BattleDamage.compute` artık `qte_bonus: float`. **Çoklu-stroke:** `battle.gd` anında submit etmez, 0.55s idle debounce (X iki çizgi çizilebilir); QTE süreleri artırıldı. **Övgü:** `qte_scored` sinyali → çizimden hemen sonra "MÜKEMMEL/HARİKA/GÜZEL/FENA DEĞİL/IŞKA + puan". Menü **maks potansiyel hasar** gösterir. **Bug fix:** `TurnDebugOverlay.update_view` null-güvenli (savaş sonu crash gitti). Test 108/108, runtime temiz. → [[Turn-Based Savaş ve QTE]], [[Rün Çizim Mekaniği]]
(önceki: **Meta ilerleme: home/characters/level select + 2 para**)

2026-09-10 — **Meta ilerleme: home/characters/level select + 2 para + kalıcı kayıt** (verified 2026-09-10): Tam meta loop. `MetaProgress` = autoload `Meta`, `user://meta.json`'a kaydeder. **İki para:** Altın (soft, bölüm sonu, tüm upgrade) + Kristal (premium/IAP stub). **Kristal sink YOK** — `convert_crystal` ile altına çevrilir (1:100, kullanıcı kararı). **Upgrade:** karakter başına CAN (+15 max HP/lv) + HASAR (+4 flat/lv), altınla, maliyet base*(lv+1); savaş başında CAN parti HP'sine, HASAR becerilere (duplicate) baklanır. **Sayfalar (kod-içi UI):** `home` (para çubuğu + OYNA/KARAKTERLER/çevir/IAP-stub/çıkış), `level_select` (5 seviye, `is_level_unlocked` kilit), `characters` (stat + yükselt). main_scene=`home`. `RunContent` seviye-bilinçli (`stage_nodes(i)` ölçekli, `reward_gold(i)`/`reward_crystal(i)`); kazanınca `battle.gd` ödülü Meta'ya yazar + `clear_level` + ANA EKRAN. Test 95/95, runtime temiz. → [[Turn-Based Savaş ve QTE]], [[Para Kazanma — Açık]]
(önceki: **Run host entegrasyonu: battle.gd RunManager'ı sürüyor**)

2026-09-10 — **Run host entegrasyonu: battle.gd RunManager'ı sürüyor (OYNANIR)** (verified 2026-09-10): Omurga artık sahneye bağlı. `scripts/battle.gd` tek-savaştan **run host**'a döndü: `RunManager` düğümleri gezer, BATTLE'da her `RunLoadout`'tan geçici Character (draft `available_skills` + run max HP) kurup `TurnManager` ile savaşır, bitince HP'yi loadout'a yazıp `report_battle_result`; CHOICE'ta rün-draft/boost butonları (`_combo_hint` "⚡AÇILIR!" ipucu) -> `apply_choice`; REWARD/bitiş ekranı. HP savaşlar arası taşınır, kazanınca düşen %25 dirilir (MVP soft-lock önleme). Yeni `RunContent` = demo bölüm (katalog+parti+`[BATTLE,CHOICE]×2→BOSS→REWARD`). Godot tip tuzağı: `Array` değişkeni tipli property'ye atanmaz -> `_enemy` paramları + `available_skills` dönüşü tiplendi. Test 71/71, `run_project` runtime TEMİZ. → [[Turn-Based Savaş ve QTE]]
(önceki: **Run omurgası: roguelite bölüm akışı + rün draftı**)

2026-09-10 — **Run omurgası: roguelite bölüm akışı + rün draftı** (verified 2026-09-10): Retention yönü belirlendi (kullanıcı): **Cup Heroes tarzı bölüm ilerleme** (Last War şehir kurma REDDEDİLDİ — DNA'ya ters). Bölüm deseni `[BATTLE, CHOICE]×N → BOSS → REWARD` → ana ekran → kalıcı güçlenme → tekrar. Yeni saf/headless katman `scripts/rpg/run/`: `RunManager` (host-callback'li, savaşı kendi sürmez — BATTLE'da düşman verir→`report_battle_result`, CHOICE'ta `apply_choice`, REWARD→para), `RunNode`/`StageDef` (düğüm listesi), `RunState`/`RunLoadout` (run-içi kit + HP taşıma), `SkillCatalog`, `ChoiceOption`/`ChoiceGenerator` (deterministik). **Rün-draft (kullanıcı seçti):** büyücüler temel rünle girer, seçimlerde rün DRAFT eder; kit her run sıfırlanır; **kaynak çifti toplanınca birleşim EMERGENT açılır** (ember+storm→Plazma). RNG yalnız seçimde, çizim saf beceri. Meta katman (kalıcı para/açılan rün) henüz YOK. Test 68/68. → [[Turn-Based Savaş ve QTE]]
(önceki: **Şarj barlı birleşim becerileri + kombo/füzyon motoru silindi**)

2026-09-10 — **Şarj barlı birleşim becerileri + kombo/füzyon motoru SİLİNDİ** (verified 2026-09-10): Beceri modeli yenilendi. Strike kaldırıldı. Her karakter **3 büyü** = 2 rün-özel NORMAL + 1 **BİRLEŞİM** (ultimate). Birleşim yeni şekil getirmez — kaynak rünler QTE'de **peş peşe** çizilir (Kayra Plazma = ember→storm, Derin Fırtına = frost→gale); tümü doğru → büyük buff (bonus ×2.5). **Şarj barı**: hasar ver+al miktarı kadar dolar (`Combatant.charge`, max 100), dolunca birleşim açılır, kullanınca sıfırlanır. **Durum sistemi**: Plazma yakma DoT (sonraki tur %40 tekrar hasar, `Skill.dot_fraction`), Fırtına stun (`applies_stun`, bir tur atla) — ikisi de `TurnManager._begin_turn`'de sıra başında işlenir. **Kombo/füzyon tamamen silindi**: eski gerçek-zamanlı motor (`main.gd`/`combo_resolver`/`damage_rules`/`resolved_spell`/`debug_overlay`), 3 test suite, tüm `deprecated/`; `RuneDB` sadeleşti (runes+shape_to_rune), `combo_config.json` küçüldü. Test 46/46. → [[Turn-Based Savaş ve QTE]]
(önceki: **Turn-based RPG'ye dönüşüm**)

2026-09-10 — **Turn-based RPG'ye dönüşüm** (verified 2026-09-10): Gerçek zamanlı dalga savunması → **turn-based parti RPG**. Rün çizimi kalır ama rolü değişti: sürekli kombo DEĞİL, beceri kullanılırken tetiklenen **sınırlı süreli QTE**. Yeni saf çekirdek `scripts/rpg/`: `TurnManager` (IDLE→SELECTING_ACTION→QTE→RESOLVING→NEXT_TURN, `tick(unscaled_dt)`, motordan bağımsız), `Character`/`Skill`/`Enemy` (Resource), `EnemyAI` stub (en düşük HP → en yüksek hasar), `BattleDamage` (fail-soft + hasar asla sıfır), `TurnDebugOverlay`. `RuneDB`/`IRuneRecognizer` yeniden kullanıldı, `FakeRecognizer` eklendi. Demo: `scenes/battle.tscn`. Eski kombo çekirdeği (`ComboStateMachine`/`ChargeMeter`/`ChainTracker`/`TimeScaleController`) → `deprecated/`. Test 79/79. → [[Turn-Based Savaş ve QTE]]
(önceki: **Yeni sürüm APK telefona kuruldu**)

2026-09-07 — **Yeni sürüm APK telefona kuruldu** (verified 2026-09-07): `build/wizard_game.apk` yeniden export (27MB→37MB, menü+UI assetleri büyüttü), `adb install -r` ile Xiaomi emerald'a kuruldu + launcher intent ile başlatıldı. Toolchain PATH'te değil, sabit yollar: Godot `~/Downloads/Godot.app/Contents/MacOS/Godot`, JDK17 `/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home`, Android SDK `/opt/homebrew/share/android-commandlinetools` (adb = `platform-tools/adb`). Build: `JAVA_HOME=... Godot --headless --path . --export-debug "Android" build/wizard_game.apk`. → log.md
(önceki: **Ana menü eklendi**)

2026-09-07 — **Ana menü eklendi** (verified 2026-09-07): `scenes/main_menu.tscn` + `scripts/main_menu.gd`. Açılış sahnesi artık menü (`project.godot` main_scene). Başlık + 3 buton (OYNA→oyun, AYARLAR→"yakında" paneli, ÇIKIŞ). Butonlar Kenney UI Pack Pixel Adventure 9-patch tile'ları (`assets/ui/*.png`, StyleBoxTexture). Global texture filter Nearest (pixel-art). → log.md
(önceki: **İlk Android APK build alındı**)

2026-09-07 — **İlk Android APK build alındı** (verified 2026-09-07): `build/wizard_game.apk` (27MB, arm64-v8a, debug-signed). Toolchain sıfırdan kuruldu (OpenJDK 17 + Android SDK build-tools;35 + export şablonları + debug keystore). Prebuilt yol (`use_gradle_build=false`). `project.godot`'a ETC2/ASTC compression zorunlu eklendi. Build komutu: `Godot --headless --path . --export-debug "Android" build/wizard_game.apk` (JAVA_HOME set). → log.md
(önceki: **Çizim juice eklendi** — `scripts/rune_trail.gd`: canlı iz.)
(önceki: **Kombo sistemi çekirdeği çalışıyor**.)

### (arşiv) Kombo sistemi çekirdeği
2026-09-07 — **Kombo sistemi çekirdeği çalışıyor**. İki katman: saf `RefCounted` çekirdek (`scripts/core/`, motordan bağımsız, `unscaled_dt` alır) + motor adaptörü (`main.gd`). Sınıflar: `ComboResolver` (saf: taşıyıcı=ilk rün, etki union+füzyon, hasar ×1.6/rün), `ComboStateMachine` (Idle→Casting→Window, overdrive), `ChargeMeter`, `ChainTracker`, `TimeScaleController`, `RuneDB`, `DamageRules`. **Kritik:** tüm kombo zamanlaması duvar saatinden (`Time.get_ticks_usec`) → `Engine.time_scale`'den bağımsız (pencere yavaşlamaz). Tuning: `data/combo_config.json`. Recognizer null→strike. Debug overlay F1. Test: `godot --headless -s res://tests/run_tests.gd` → 56/56 geçti. → [[Kombo ve Palet]]
(önceki: **Tank melee düşman** — `scripts/enemy.gd`, yürü+menzilde melee, HP 400.)

### (arşiv) Tank melee düşman
2026-09-07 — **Tank melee düşman çalışıyor**. Yeniden kullanılabilir `Enemy` node'u (`scripts/enemy.gd`, preload + `setup`/`tick`, health.gd pattern'i). Gövdeyi oyuncuya doğru yürütür, menzile girince melee vurur. Tank profili: HP 400, hasar 5, hız 45 px/sn, cooldown 1.4 sn. Oyuncu artık gerçekten hasar alıyor; saldırı yalnız menzilde (eski uzaktan-hasar bug'ı yok). Can çubuğu yürüyen tankı takip eder. run_project ile test: yürüdü/vurdu/400 HP'de öldü, hata yok. → [[Düşman Tasarımı]], [[Can ve Hasar Sistemi]]
(önceki: `Health`+`HealthBar` bileşenleri, 100 HP, mermi hasarı 10–30; 4 rün tanıma + renkli mermi.)

## Key Recent Facts
- **Görsel/ritim güncel yönü (2026-09-16):** [[Tatlış Büyücüler ve Açık Hava Görsel Dili]] geçerli. Aşağıdaki koyu arka plan, kayan piano-tile şeridi, yalnız tint ve düz menü notları tarihsel durumu anlatır.
- **YÖN DEĞİŞİKLİĞİ (2026-09-10, güncellendi 2026-09-13 REDESIGN):** oyun **turn-based roguelite RPG** (Cup Heroes tarzı). Gerçek-zamanlı motor + rün çizimi SİLİNDİ. **Çizim yok ama AKTİF GİRDİ var** — her beceri kısa tap/swipe dizisi (`InputSequence`), saf `InputEvaluator` PERFECT/GOOD/MISS üretir, **fail-soft** (MISS bile taban hasar). Çekirdek `scripts/rpg/` (`TurnManager` `AWAITING_INPUT`+`submit_input`, `MageForm`, `Skill`, `Combatant`, `Enemy`, `EnemyAI`, `BattleDamage`, `Relic`/`RelicSet`, `Equipment`). Aşağıdaki gerçek-zamanlı + QTE/çizim + tap-to-cast + 6-combo satırları **tarihsel — geçersiz**. → [[Turn-Based Savaş ve QTE]]
- **Beceri/form modeli (güncel 2026-09-13 REDESIGN):** **kimlik-swap**. MVP tek başlangıç = **Kor Büyücü (Ember form)**: temel büyü Ateş Oku (yakma pasifi=`dot_fraction`), ultimate İnferno (AoE+yakma), girdi SOL→SAĞ→DOKUN. CHOICE'ta **Storm rünü alınca Plazma'ya DÖNÜŞÜR** — temel büyü/girdi(DOKUN→DOKUN→SAĞ)/ultimate(Plazma Fırtınası, AoE+stun) hepsi değişir (combo matrisi YOK). Ultimate şarj barı dolunca seçilir (hasar ver+al doldurur, wave-arası %20 decay). **Orb** yenilen düşman başına 1 → CHOICE'ta fizik board → çarpılmış orb → kart bedeli. **Relic** (run) + **Ekipman** (meta, 9 parça/3 slot) run/savaş kurallarını hook'larla değiştirir. Run = `[BATTLE,CHOICE]×3→BOSS→REWARD`. → [[Turn-Based Savaş ve QTE]]
- **Rün Büyücüsü** — dikey 2D dalga savunması. Alt-orta sabit kareye rün çizilir → soldaki büyücü sağdaki düşmana fırlatır. Rünler zincirlenip kombo yapar. → [[Rün Büyücüsü — Konsept]]
- Fark: girdi = üretim (seçim değil), "ben yaptım" hissi. Reklam = oynanış (dilsiz, 6 sn).
- **Onaylı kararlar:** sabit çizim karesi ([[Ekran Düzeni]]), savaşa 4 rün ([[Loadout Kısıtı]]), sessiz başarısızlık yok ([[Sessiz Başarısızlık Yok]]), zaaf bonus %40 ([[Zaaf Bonustur, Kapı Değil]]).
- **Bilişsel yük 4 kolonu:** loadout, çizerken önizleme, sessiz başarısızlık yok, rün formu kısıtları (2–3 çizgi, köşeli, ayrık silüet). → [[Rün Çizim Mekaniği]]
- **(tarihsel — strike kaldırıldı 2026-09-10)** ~~Düz vuruş (strike) = draw alanına tık → anında taban.~~ Turn-based sistemde strike yok; başarısız QTE zaten fail-soft taban hasar veriyor.
- Tanıma: **$1 recognizer** aday (araştırılacak). Karışma matrisine göre rün ayır.
- **Can/hasar var** (`scripts/health.gd`, `health_bar.gd`): tekrar kullanılır `Health`/`HealthBar`. Kombo hasar çarpanı + juice + dalga henüz yok. → [[Can ve Hasar Sistemi]]
- **Düşman + dalga sistemi var** (`scripts/enemy.gd`, `main.gd`): 3 arketip `ENEMY_TYPES` (tank melee / swarm hızlı-melee / okçu ranged mermi atar). `WAVES` sıralı dalga — temizlenince sonraki spawn, son dalga → `game_won`. Test bölümü: 3 dalga (1t+10s / 3t+2o / 2t+2o+5s). Silüetten zaaf okuma + zafer/yenilgi UI eksik. → [[Düşman Tasarımı]]

## Open Tasks
- **Savaş görselleri BAĞLANDI (2026-09-12; büyücü sprite güncel 2026-09-13):** arka plan (`battle_bg.png`), parti = `wizard.tscn` AnimatedSprite2D, düşmanlar = 3 sprite seti (`assets/enemies/{dragon,goblin,dev}`, `_enemy_sprite_key` ile eşlenir, `flip_h` party'e dönük). Can+enerji barları (yazı yok), grade övgüsü İngilizce+pixel font (PixelOperator8-Bold). **Büyücü artık gerçek pixel-art (2026-09-13):** `assets/wizard/wizard_fire_frames.tres` (Ember/Kor) + `wizard_arcane_frames.tres` (Plazma), anim'ler idle/cast/hurt/death/victory/ult. `battle.gd` `PLAYER_FRAMES`+`_player_frames(c)` sprite'ı **güncel formun sprite_key'inden** seçer (ember→fire, plasma→arcane); `_on_transformed` dönüşümde canlı sprite'ı swap edip "victory" oynatır. **Anim timing (2026-09-13):** savaş sonu artık `BATTLE_END_PAUSE`(1.6s) bekler (`_pending_end`/`_finalize_battle`) → ölüm/zafer/ult anim'leri oynar (eskiden `report_battle_result` senkron çağrılıp anında node değişiyordu, hiçbir şey görünmüyordu). Ultimate ayrı "ult" anim'i (`_last_skill.requires_charge`). **SpellFX (2026-09-13):** büyü efektleri `Skill.carrier`+`rune_id`'ye göre — Projectile=fireball/orb tween, Beam=plazma ışın, Area=rainbow meteor+ateş AoE, Storm=mor şimşek AoE (`assets/wizard/fx_*.png`, hibrit: sheet'ten çıkarılmış sprite + prosedürel tween). Impact varışında hurt+yükselen hasar sayısı+patlama. Kalan: düşman atlas artefaktları; **LvlUp→transform'da mor ULT beam'in ayrı "plazma" overlay'i (kullanıcı planı "sonra")**; konum/ölçek telefonda son kontrol.
- **(tarihsel — 2026-09-16 revizyonuyla güncellendi) UI cilası (kod-içi, düz):** home/characters/level_select/battle hepsi kod-içi düz buton — Kenney UI asset'leri (main_menu'deki gibi) uygulanmadı. CHOICE ekranı + boss düğümü görsel ayrışmıyor. Hedef seçim hâlâ otomatik (en düşük HP).
- **Gerçek IAP:** KRİSTAL AL şu an test stub (+5). Gerçek mağaza entegrasyonu yok.
- **Açılan rün havuzu (opsiyonel):** kullanıcının erken bahsettiği "kristalle rün aç" fikri kurulmadı (kristal şu an sadece altına çevriliyor).
- **Denge:** upgrade maliyet/step, seviye ölçek (`hp_scale`/`dmg_scale` `run_content.gd`), ödül miktarları (80+40i) tuning edilmedi — prototip değerleri. 20-seviye eğrisi telefonda gerçek upgrade akışıyla test edilip soft-gate sertliği ayarlanmalı.
- **QTE skorlama tuning:** `RuneTemplates.MAX_DIST`=0.40, `BattleConfig.qte_hit_threshold`=0.45, `qte_speed_floor`=0.70, `SUBMIT_DEBOUNCE`=0.55s — telefonda gerçek parmakla test edilip ayarlanmalı (çok sıkı/gevşek mi?). QTE süreleri (ember 3.2s) de.
- Zafer/yenilgi ekranı (şu an sadece `print`); dalga arası nefes/gösterge.
- [[Prototip M0]] yap: 4 rün + düz çizgi, swarm+tank+okçu, ~30 sn. Telefonda, tek elle. (dalga sistemi hazır)
- Test: birine 20 dk oynat → **20. dk'da düşünerek mi refleksle mi çiziyor?** (D7 tekrar riski ölçümü)

## Open Bugs
- _(none yet)_

## Kararı Verilmeyen (tahmin üretme)
- Para kazanma: **iki-para modeli KARAR VERİLDİ 2026-09-10** (Altın soft + Kristal premium, kristal→altın çevrim, upgrade'ler altınla → log.md). AÇIK kalan: gerçek IAP fiyatlandırma, beceri↔ödeme çelişkisi (pay-to-win riski), kristalin çevrim dışı değeri. → [[Para Kazanma — Açık]]
- Geri dönüş kancası (günlük ödül/challenge), sosyal. (Retention omurgası KARAR VERİLDİ 2026-09-10: Cup Heroes tarzı bölüm ilerleme + run-draft → log.md.)
- Bölüm sayısı/zorluk eğrisi **KARAR VERİLDİ 2026-09-12** (20 seviye, 4 tier, `_LEVELS` tablosu → [[Turn-Based Savaş ve QTE]] "Seviye Eğrisi"). AÇIK: REWARD para miktarı + meta upgrade maliyet dengesi (soft-gate sertliği).
- Kombo penceresi süresi artık `data/combo_config.json`'da — prototipte ayarlanacak, mimari karar değil.
